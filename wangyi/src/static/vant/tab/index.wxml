<view
  wx:if="{{ animated || inited }}"
  class="custom-class van-tab__pane {{ animated ? 'van-tab__pane--float' : '' }}"
  style="{{ animated || active ? '' : 'display: none;' }} {{ width ? 'width:' + width + 'rpx;' : '' }}"
>
  <slot />
</view>
